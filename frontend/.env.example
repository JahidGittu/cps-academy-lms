# Where the Strapi API lives. NEXT_PUBLIC_ because the browser is what calls it: auth is a bearer
# token in localStorage, so the requests are made from the client, not from a Next server.
NEXT_PUBLIC_API_URL=http://localhost:1337
